<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title><%= user.username %> • Relm</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>

  <body class="min-h-screen bg-zinc-900 text-white">
    <main class="max-w-3xl mx-auto px-6 py-10">
        <a href="/home" class="text-2xl font-semibold tracking-tight inline-block mb-4">
        <span class="text-blue-400">R</span>elm
        </a>
      <header class="flex flex-col sm:flex-row items-start sm:items-center justify-between gap-6 mb-8">
        <div class="flex items-start sm:items-center gap-4">
          <img
            src="<%= user.image %>"
            alt="avatar"
            class="w-14 h-14 sm:w-16 sm:h-16 rounded-full bg-zinc-700 object-cover"
          />
          <div class="min-w-0"> 
            <h1 class="text-lg sm:text-2xl font-semibold tracking-tight truncate">
              <%= user.name || user.username %>
            </h1>
            <p class="text-sm text-zinc-400">@<%= user.username %></p>
            <p class="text-sm text-zinc-400 mt-1 max-w-xs sm:max-w-none break-words">
              <%= user.bio || '' %>
            </p>
          </div>
        </div>

        <div class="flex items-center gap-2 mt-4 sm:mt-0">
          <a
            href="/posts/new"
            class="px-4 py-2 rounded-lg text-sm bg-gradient-to-r from-blue-600 to-indigo-600 hover:opacity-95"
          >
            New post
          </a>

          <a
            href="/profile/edit"
            class="px-4 py-2 rounded-lg text-sm border border-zinc-700 hover:bg-zinc-800"
          >
            Edit profile
          </a>
        </div>
      </header>


      <div class="flex items-center justify-between text-sm text-zinc-400 mb-6">
        <div>
          <span class="font-medium text-zinc-200"
            ><%= posts ? posts.length : 0 %></span
          >
          <span class="ml-1">posts</span>
        </div>
        <div>
          <span
            >Member since
            <strong class="text-zinc-200">
              <%= dayjs(user.createdAt).format("DD MMM YYYY") %>
            </strong>
          </span>
        </div>
      </div>

      <nav class="flex gap-6 border-b border-zinc-800 pb-3 mb-6 text-sm">
        <span class="pb-1 border-b-2 border-blue-500 font-medium">Posts</span>
        <a href="/profile/about" class="text-zinc-400 hover:text-white"
          >About</a
        >
        <a href="/profile/settings" class="text-zinc-400 hover:text-white"
          >Settings</a
        >
      </nav>

      <section class="space-y-6">
        <% if (posts && posts.length > 0) { %> <% posts.reverse().forEach((post) => { %>
        <article
          class="p-5 rounded-xl bg-zinc-800/60 hover:bg-zinc-800 transition"
        >
          <a href="/posts/<%= post._id %>" class="block">
            <h2 class="text-lg font-medium tracking-tight mb-2">
              <%= post.title %>
            </h2>

            <p class="text-sm text-zinc-400 mb-4 whitespace-pre-line">
              <% const preview = post.content ? String(post.content) : ''; const
              short = preview.length > 230 ? preview.substring(0,230) + '...' :
              preview; %> <%= short %>
            </p>
          </a>

          <div class="flex items-center justify-between text-xs text-zinc-500">
            <time>
               <%= dayjs(post.createdAt).format("DD MMM • HH:mm") %>
            </time>

            <div class="flex items-center gap-4">
              <div class="flex items-center gap-1">
                <svg
                  xmlns="http://www.w3.org/2000/svg"
                  class="w-4 h-4"
                  fill="none"
                  viewBox="0 0 24 24"
                  stroke="currentColor"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="1.5"
                    d="M14 9l-2 2-2-2m4 6l-2 2-2-2"
                  />
                </svg>
                <span
                  ><%= Array.isArray(post.likes) ? post.likes.length : 0
                  %></span
                >
              </div>

              <a
                href="/posts/<%= post._id %>"
                class="text-zinc-400 hover:text-white"
                >Read</a
              >
            </div>
          </div>
        </article>
        <% }) %> <% } else { %>
        <div class="text-sm text-zinc-500">
          You haven't posted yet, start writing your first post.
        </div>
        <% } %>
      </section>

      <footer class="mt-12 text-xs text-zinc-500">
        Logged in as <%= user.email %> ·
        <a href="/auth/signout" class="text-red-400 hover:underline ml-2"
          >Logout</a
        >
      </footer>
    </main>
  </body>
</html>
