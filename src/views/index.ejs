<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register • Relm</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body
    class="min-h-screen bg-zinc-900 text-white flex items-center justify-center p-6 tracking-tight"
  >
    <main class="w-full max-w-md">
                <a href="/home" class="text-2xl font-semibold tracking-tight inline-block mb-4">
        <span class="text-blue-400">R</span>elm
      </a>
      <section
        class="bg-zinc-800/70 backdrop-blur-sm rounded-2xl shadow-lg p-8"
      >
        <h1 class="text-2xl font-semibold mb-4 text-center">
          Create an account
        </h1>
        <p class="text-sm text-zinc-300 mb-6 text-center">
          Join and start creating posts
        </p>

        <form action="/createUser" enctype="multipart/form-data" method="POST" class="space-y-4">
          <div class="flex flex-col items-center gap-2">
            <label class="text-sm text-zinc-300">Profile picture</label>
            <label
              for="image"
              id="imageLabel"
              class="w-24 h-24 rounded-full bg-zinc-700/60 flex items-center justify-center cursor-pointer hover:bg-zinc-600 transition"
            >
            <span class="text-xs text-zinc-300">Upload</span>
            </label>
            <input
              id="image"
              name="image"
              type="file"
              accept="image/*"
              class="hidden"
            />
          </div>

          <div>
            <label for="username" class="block text-sm mb-1">Username</label>
            <input
              id="username"
              name="username"
              type="text"
              required
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="yourusername"
            />
          </div>

          <div>
            <label for="name" class="block text-sm mb-1">Full name</label>
            <input
              id="name"
              name="name"
              type="text"
              required
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="Alex Carter"
            />
          </div>

          <div>
            <label for="email" class="block text-sm mb-1">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              required
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="you@example.com"
            />
          </div>

          <div>
            <label for="password" class="block text-sm mb-1">Password</label>
            <input
              id="password"
              name="password"
              type="password"
              required
              minlength="6"
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="••••••••"
            />
          </div>

          <div>
            <label for="dateOfBirth" class="block text-sm mb-1"
              >Date of birth</label
            >
            <input
              id="dateOfBirth"
              name="dateOfBirth"
              type="date"
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
            />
          </div>
          <button
            type="submit"
            class="w-full mt-2 py-2 rounded-lg font-medium text-sm bg-gradient-to-r from-blue-600 to-indigo-600 hover:opacity-95"
          >
            Create account
          </button>
        </form>

        <p class="text-center text-xs text-zinc-400 mt-4">
          Already have an account?
          <a href="/auth/signin" class="text-blue-400 hover:underline"
            >Sign in</a
          >
        </p>
      </section>

      <footer class="text-center text-xs text-zinc-500 mt-4">
        By continuing you agree to the terms.
      </footer>
    </main>
    <script>
            const input = document.getElementById("image");
      const label = document.getElementById("imageLabel");

      input.addEventListener("change", () => {
        const file = input.files[0];
        if (!file) return;

        const url = URL.createObjectURL(file);
        label.style.backgroundImage = `url(${url})`;
        label.style.backgroundSize = "cover";
        label.style.backgroundPosition = "center";
        label.textContent = "";
      });
    </script>
  </body>
</html>
