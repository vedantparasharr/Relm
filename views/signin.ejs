<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sign In</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body
    class="min-h-screen bg-zinc-900 text-white flex items-center justify-center p-6"
  >
    <main class="w-full max-w-md tracking-tighter">
      <section
        class="bg-zinc-800/70 backdrop-blur-sm rounded-2xl shadow-lg p-8"
      >
        <h1 class="text-2xl font-semibold mb-4 text-center">
          Sign in to your account
        </h1>
        <p class="text-sm text-zinc-300 mb-6 text-center">
          Welcome back, enter your details to continue
        </p>

        <form action="/auth/signin" method="POST" class="space-y-4">
          <div>
            <label for="email" class="block text-sm mb-1">Email</label>
            <input
              id="email"
              name="email"
              type="email"
              required
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="you@example.com"
            />
          </div>

          <div>
            <label for="password" class="block text-sm mb-1">Password</label>
            <input
              id="password"
              name="password"
              type="password"
              required
              minlength="6"
              class="w-full px-3 py-2 rounded-lg bg-zinc-900/40 border border-zinc-700 placeholder-zinc-400 outline-none focus:ring-2 focus:ring-blue-500"
              placeholder="••••••••"
            />
          </div>

          <div class="flex items-center justify-between text-sm">
            <label class="inline-flex items-center gap-2">
              <input
                type="checkbox"
                name="remember"
                class="h-4 w-4 rounded border-zinc-600 bg-zinc-900 focus:ring-2 focus:ring-blue-500"
              />
              <span class="text-zinc-300">Remember me</span>
            </label>
            <a href="#" class="text-sm text-blue-400 hover:underline"
              >Forgot password?</a
            >
          </div>

          <button
            type="submit"
            class="w-full mt-2 py-2 rounded-lg font-medium text-sm bg-gradient-to-r from-blue-600 to-indigo-600 hover:opacity-95"
          >
            Sign in
          </button>
        </form>

        <p class="text-center text-xs text-zinc-400 mt-4">
          Don't have an account?
          <a href="/" class="text-blue-400 hover:underline">Create one</a>
        </p>
      </section>

      <footer class="text-center text-xs text-zinc-500 mt-4">
        By continuing you agree to the terms.
      </footer>
    </main>
  </body>
</html>
